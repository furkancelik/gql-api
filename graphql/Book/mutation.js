module.exports = {
  Mutation: {
    createBook: () => {
      return { title: "asxd" };
      // return books.map((v, i) => {
      //   return { ...v, author: authors.find(a => a.id === v.author) };
      // });
    }
  }
};
